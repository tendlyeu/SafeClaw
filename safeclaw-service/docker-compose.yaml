services:
  safeclaw:
    build: .
    ports:
      - "8420:8420"
    volumes:
      - safeclaw-data:/home/safeclaw/.safeclaw
    environment:
      - SAFECLAW_LOG_LEVEL=INFO
      - SAFECLAW_ADMIN_PASSWORD=${SAFECLAW_ADMIN_PASSWORD:?Set SAFECLAW_ADMIN_PASSWORD}

volumes:
  safeclaw-data:
